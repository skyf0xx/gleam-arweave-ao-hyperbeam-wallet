{
  "id": "upload-submit-relocation",
  "goal": "Relocate packages/core/src/policy/upload-submit.ts to packages/core/src/arweave/upload.ts, mirroring the existing core/arweave/transfer.ts pattern -- this is core/arweave-shaped ANS-104 DataItem build/submit logic that was only ever housed in policy/ because the upload task's ALLOWED SCOPE didn't grant core/arweave/**. Resolves debt declared on GLEAM-WALLET-UPLOAD.",
  "outcome": "packages/core/src/arweave/upload.ts contains the ANS-104 DataItem build/submit logic currently in packages/core/src/policy/upload-submit.ts, unchanged in behavior, sitting alongside transfer.ts in the same directory and following the same pure-function, no-chrome.*/window/document shape. packages/core/src/policy/upload-submit.ts is removed, and every import site (the upload handler, and any test file) is updated to the new path. No behavior changes -- this is a pure relocation, not a rewrite.",
  "priority": 11,
  "requirements": [
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-RULE-1",
      "kind": "rule",
      "statement": "This is a pure move: the function signatures, exports, and internal logic of upload-submit.ts are unchanged by the relocation. Any refactor beyond the move itself (renaming exports, restructuring internals) is out of scope for this task."
    },
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-RULE-2",
      "kind": "rule",
      "statement": "packages/core/src/policy/upload-submit.ts is deleted once its logic is relocated -- the file does not remain as a re-export shim or dead duplicate."
    },
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-RULE-3",
      "kind": "rule",
      "statement": "Every existing import of the relocated module (apps/extension/src/handlers/upload.ts and its colocated test upload.upload.test.ts, plus any other import site found by a workspace-wide search) is updated to import from packages/core/src/arweave/upload.ts."
    },
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-RULE-4",
      "kind": "rule",
      "statement": "The relocated module continues to satisfy core-design.md's core-package rule: no chrome.*/window/document dependency, enforced by the existing ESLint no-restricted-imports rule (GLEAM-WALLET-RULE-1, unchanged)."
    },
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-ACCEPTANCE-1",
      "kind": "acceptance",
      "statement": "packages/core/src/policy/ no longer contains upload-submit.ts, and packages/core/src/arweave/upload.ts exists with the same exported behavior -- verified by the existing upload-behavior test suite passing unmodified in content (only its import path may change)."
    },
    {
      "id": "UPLOAD-SUBMIT-RELOCATION-ACCEPTANCE-2",
      "kind": "acceptance",
      "statement": "pnpm vitest run policy upload arweave passes; pnpm tsc -p packages/core --noEmit passes; pnpm wxt build succeeds (this module is bundled transitively via the background entrypoint, per core-design.md's polyfill Correction Protocol entry)."
    }
  ],
  "depends_on": [
    "forgot-password-wire-contract"
  ]
}